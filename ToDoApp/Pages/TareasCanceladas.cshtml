@page
@model TareasCanceladasModel
@{
    ViewData["Title"] = "Tareas Canceladas";
}

<div class="container-fluid">
    <h1><i class="fas fa-ban me-2"></i>Tareas Canceladas</h1>

    @if (!string.IsNullOrEmpty(Model.MensajeError))
    {
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <strong>Error:</strong> @Model.MensajeError
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    }

    <div class="table-responsive mb-3">
        <table class="table">
            <thead>
                <tr>
                    <th>Nombre Tarea</th>
                    <th>Fecha Vencimiento</th>
                    <th>Estado</th>
                    <th class="text-end">Acciones</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.TareasCanceladas != null && Model.TareasCanceladas.Any())
                {
                    @foreach (var tarea in Model.TareasCanceladas)
                    {
                        <tr>
                            <td>@tarea.Nombre</td>
                            <td>@tarea.FechaFormateada()</td>
                            <td><span class="badge @tarea.BadgeClass()">@tarea.Estado</span></td>
                            <td class="text-end">
                                <button class="btn btn-sm btn-outline-primary"><i class="fas fa-redo"></i></button>
                                <button class="btn btn-sm btn-danger"><i class="fas fa-trash"></i></button>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr>
                        <td colspan="4" class="text-center py-3">
                            <div class="d-flex flex-column align-items-center">
                                <i class="fas fa-thumbs-up fa-2x text-success mb-2"></i>
                                <p class="m-0">No hay tareas canceladas.</p>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    @if (Model.Paginacion.TotalTareas > 0)
    {
        <partial name="_Paginacion" model="Model.Paginacion" />
    }
</div>